/**
 * Single source of truth for app display name and version.
 * Bump APP_VERSION when releasing; keep in sync with package.json version if desired.
 */
export const APP_NAME = 'Sunspire';
export const APP_VERSION = '0.1.0';
